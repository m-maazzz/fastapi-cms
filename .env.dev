DB_DRIVER=postgresql
DB_USER=postgres
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fastapi_cms_db

DATABASE_URL=${DB_DRIVER}://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

secret_key=your_secret_key
algorithm=HS256

TABLE_PREFIX=tbl_
